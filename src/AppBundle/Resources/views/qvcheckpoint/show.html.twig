{% extends 'AppBundle::base.html.twig' %}

{% block body %}
    <h1>qvCheckpoint</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ qvCheckpoint.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ qvCheckpoint.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('checkpoint_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('checkpoint_edit', { 'id': qvCheckpoint.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
