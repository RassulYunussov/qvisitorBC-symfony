{% extends 'AppBundle::base.html.twig' %}

{% block body %}
    <h1>qvRole</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ qvRole.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ qvRole.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('role_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('role_edit', { 'id': qvRole.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
