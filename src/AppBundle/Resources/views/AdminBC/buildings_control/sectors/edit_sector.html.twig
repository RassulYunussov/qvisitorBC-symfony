{% extends 'AppBundle::base.html.twig' %}


{% block title %}Личный кабинет администратора{% endblock %}


{% block body %}

    <div id="page-wrapper">
		<div class="row">
			<div class="col-lg-12">
				{% block breadcrumb %}
					 <div id="breadcrumbs" class="btn-group btn-breadcrumb">
                        <br>
                        <a class="btn btn-default" href="{{ path('main_page') }}">
                        <i class="fa fa-home"></i>Главная
                        </a>
		<a class="btn btn-default" href="{{ path('buildings_list') }}">Управление зданиями</a>
		<a class="btn btn-default" href="{{ path('buildings_show', {'id': qvBuilding.id}) }}">Управление этажами</a>
		<a class = "btn btn-default" href="{{ path('floors_show', {'qvBuilding': qvBuilding.id, 'id':qvFloor.id}) }}">Управление секторами</a>
		<a class="btn btn-default" href="{{ path('sectors_show', {'qvBuilding': qvBuilding.id, 'qvFloor':qvFloor.id, 'id':qvSector.id }) }}'">Просмотр информации о секторе</a>
					</div>
				{% endblock %}

				<h1 class="page-header">Изменить информацию о секторе</h1>
				<!-- /.col-lg-12 -->
				</div>
   
			<div class="col-lg-6">
				<div class="panel panel-default">
					<div class="panel-body">
						<div class="row">
							<div class="col-lg-8">
									<ul>			
    {{ form_start(edit_form) }}
{{ form_row(edit_form.name) }}
<br>
<div class="form-input" align="right">
<table>
	<tbody>
	<tr>
	<td>  <input class="btn btn-default btn-sm" type="submit" value="Изменить" /></td>
            <td><a  class="btn btn-default btn-sm" href="{{ path('floors_show', {'qvBuilding': qvSector.floor.building.id, 'id': qvSector.floor.id}) }}">Отменить</a></td>
    {{ form_end(edit_form) }}
 {{ form_start(delete_form) }}
                <td><input type="submit" class="btn btn-default btn-sm" value="Удалить"></td>
            {{ form_end(delete_form) }}
        </tr>
        </tbody>
</table>   
    </div>		
							</div>
							<!-- /.col-lg-6 (nested) -->
						</div>
						<!-- /.row (nested) -->
					</div>
					<!-- /.panel-body -->
				</div>
				<!-- /.panel -->
			</div>
   
        <!-- /.row -->
        </div>
    <!-- /#page-wrapper -->
	</div>
    


{% endblock %}