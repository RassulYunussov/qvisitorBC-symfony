{% extends 'AppBundle::base.html.twig' %}


{% block title %}Личный кабинет администратора{% endblock %}


{% block body %}
    <div id="page-wrapper">
		<div class="row">
			<div class="col-lg-12">
				{% block breadcrumb %}
						 <div id="breadcrumbs" class="btn-group btn-breadcrumb">
	                        <br>
	                        <a class="btn btn-default" href="{{ path('main_page') }}">
	                        <i class="fa fa-home"></i>Главная
	                        </a>
							<a class="btn btn-default" href="{{ path('buildings_list') }}">Управление зданиями</a>
								<a class="btn btn-default" href="{{ path('buildings_show',{'id':qvBuilding.id}) }}">Управление этажами</a>
								<a class = "btn btn-default" href="{{path('floors_show', {'qvBuilding':qvBuilding.id, 'id':qvFloor.id})}}">Управление секторами</a>
						</div>
				{% endblock %}

				<h1 class="page-header">Создание нового сектора</h1>
				<!-- /.col-lg-12 -->
				</div>
   
			<div class="col-lg-8">
				<div class="panel panel-default">
					
					<div class="panel-body">
						<div class="row">
							<div class="col-md-6">
								     <ul>
                {{ form_start(form) }}
            {{ form_row(form.name) }}
    <br>
    <div class="form-inline form-input">
    	<table>
    		<tbody>
    			<tr>
    		<td>
                <button type="submit" class="btn btn-default btn-sm">
                    Создать
                </button></td>
              <td>
                <a href="{{ path('floors_show', {'qvBuilding': qvBuilding.id, 'id':qvFloor.id}) }}" class="btn btn-default btn-sm">
                    Отменить
                </a>
            </td>
                {{ form_end(form) }}
            </tr>
        </tbody>
    </table>
						</div> 
						</div>
							<!-- /.col-lg-6 (nested) -->
						</div>
						<!-- /.row (nested) -->
					</div>
					<!-- /.panel-body -->
				</div>
				<!-- /.panel -->
			</div>
   
        <!-- /.row -->
        </div>
    <!-- /#page-wrapper -->
	</div>
    


{% endblock %}