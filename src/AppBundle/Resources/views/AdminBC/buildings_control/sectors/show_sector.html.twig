{% extends 'AppBundle::base.html.twig' %}


{% block title %}Личный кабинет администратора{% endblock %}

{% block body %}

    <div id="page-wrapper">
		<div class="row">
			<div class="col-lg-12">
				{% block breadcrumb %}
					  <div id="breadcrumbs" class="btn-group btn-breadcrumb">
                        <br>
                        <a class="btn btn-default" href="{{ path('main_page') }}">
                        <i class="fa fa-home"></i>Главная
                        </a>
                            <a class="btn btn-default" href="{{ path('buildings_list') }}">Управление зданиями</a>
                            
                            <a class="btn btn-default" href="{{ path('buildings_show',{'qvBuilding':qvBuilding.id}) }}">{{ qvBuilding.name }}
                        </a>
								<a class="btn btn-default" href="{{ path('buildings_show',{'qvBuilding':qvBuilding.id}) }}">Управление этажами</a>
								<a class="btn btn-default" href="{{ path('floors_show',{'qvBuilding':qvBuilding.id, 'id':qvFloor.id}) }}">{{ qvFloor.name }}
                        </a>
                        </div>
				{% endblock %}

				<h1 class="page-header">Сектор № {{ qvSector.name }}</h1>
				<!-- /.col-lg-12 -->
				</div>
   
			<div class="col-lg-12">
				<div class="panel panel-default">
					
					<div class="panel-body">
						<div class="row">
							<div class="col-lg-6">
								<div class="panel panel-default">
                        <div class="panel-heading">
                            <i class="fa fa-bar-chart-o fa-fw"></i> Информация о секторе
                        </div>
                        <!-- /.panel-heading -->
                        <div class="panel-body">
						
						<div class="table-responsive">
                                <table class="table">
                                    
                                    <tbody>
                                        <tr class="success">
                                          	<tr class="success">
											</tr>
											<tr class="success">
												<td>Номер этажа</td>
												<td>{{ qvSector.floor.name }}</a></td>
											</tr>
<tr>
<td></td>
<td align="right"><br>
	
	<a href="{{ path('sectors_edit', {'qvBuilding':qvBuilding.id,'qvFloor':qvFloor.id,'id':qvSector.id}) }}" class="btn btn-warning">Редактировать</a>

</td>
											</tr>
                                    </tbody>
                                </table>
								
                            </div>
					</div>	
                        <!-- /.panel-body -->
                    </div>
							</div>
							<!-- /.col-lg-6 (nested) -->
						</div>
						<!-- /.row (nested) -->
					</div>
					<!-- /.panel-body -->
				</div>
				<!-- /.panel -->
			</div>
   
        <!-- /.row -->
        </div>
    <!-- /#page-wrapper -->
	</div>
    


{% endblock %}