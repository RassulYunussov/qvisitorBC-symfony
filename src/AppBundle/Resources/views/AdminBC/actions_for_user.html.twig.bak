{% block actions %}
<td class="center">
                                                <div class="btn-group">
                                    <button type="button" class="btn btn-default btn-xs dropdown-toggle" data-toggle="dropdown" aria-expanded="false">
                                        Выберите действие
                                        <span class="caret"></span>
                                    </button>
                                    <ul class="dropdown-menu pull-right" role="menu">
                                        <li><a href="{{ path('show_leaser', { 'id': qvLeaser.id }) }}">Информация об учетной записи пользователя</a>
                                        </li>
                                        <li><a href="{{ path('edit_leaser', { 'id': qvLeaser.id }) }}">Изменить информацию о пользователе</a>
                                        </li>
                                        <li><a href="{{ path('') }}">Посмотеть контракты пользователя</a>
                                        </li>
                                        <li><a href="{{ path('delete_leaser', { 'id': qvLeaser.id }) }}">Удалить пользователя</a>
                                        </li>
                                        </ul>
                                </div></td>
								{% endblock%}