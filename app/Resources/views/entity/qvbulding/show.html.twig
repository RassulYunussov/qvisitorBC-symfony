{% extends 'base.html.twig' %}

{% block body %}
    <h1>Entity\qvbulding</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity\qvbulding.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('entity_qvbulding_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('entity_qvbulding_edit', { 'id': entity\qvbulding.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
