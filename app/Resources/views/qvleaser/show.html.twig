{% extends 'base.html.twig' %}

{% block body %}
    <h1>qvLeaser</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ qvLeaser.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ qvLeaser.name }}</td>
            </tr>
            <tr>
                <th>Bin</th>
                <td>{{ qvLeaser.bin }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('leaser_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('leaser_edit', { 'id': qvLeaser.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
