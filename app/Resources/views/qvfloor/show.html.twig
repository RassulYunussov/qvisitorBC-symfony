{% extends 'base.html.twig' %}

{% block body %}
    <h1>qvFloor</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ qvFloor.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ qvFloor.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('floor_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('floor_edit', { 'id': qvFloor.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
