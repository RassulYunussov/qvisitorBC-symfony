{% extends 'base.html.twig' %}

{% block body %}
    <h1>qvGender</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ qvGender.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ qvGender.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('gender_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('gender_edit', { 'id': qvGender.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
