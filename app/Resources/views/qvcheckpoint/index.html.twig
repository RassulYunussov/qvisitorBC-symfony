{% extends 'base.html.twig' %}

{% block body %}
    <h1>qvCheckpoint list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for qvCheckpoint in qvCheckpoints %}
            <tr>
                <td><a href="{{ path('checkpoint_show', { 'id': qvCheckpoint.id }) }}">{{ qvCheckpoint.id }}</a></td>
                <td>{{ qvCheckpoint.name }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('checkpoint_show', { 'id': qvCheckpoint.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('checkpoint_edit', { 'id': qvCheckpoint.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('checkpoint_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
